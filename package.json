{
  "name": "trash-panda",
  "version": "1.0.0",
  "description": "my waste collection schedule for this week",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rm -rf build",
    "build": "parcel build index.html --public-url ./ --out-dir build",
    "prestart": "rm -rf build",
    "start": "parcel index.html -p 3000 --out-dir build",
    "deploy": "gh-pages -d build"
  },
  "author": "Varun Vachhar",
  "license": "MIT",
  "dependencies": {
    "folktale": "^2.0.1",
    "ramda": "^0.25.0"
  },
  "devDependencies": {
    "babel-preset-env": "^1.7.0",
    "gh-pages": "^1.1.0",
    "parcel-bundler": "^1.11.0"
  }
}
